<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.shj.code.mapper.UserTemplateInfoMapper">
	<select id="getUserList" resultType="com.shj.code.entity.UserTemplateInfo">
	    SELECT tf.`id`,tt.`fileName`,tt.`sourceUrl` FROM user_template_info tf LEFT JOIN template_info tt ON tf.`templateId` = tt.`id` WHERE tf.`userId` = #{userId,jdbcType=VARCHAR} AND tt.`fileName` LIKE '%${search}%'
	</select>
</mapper>
